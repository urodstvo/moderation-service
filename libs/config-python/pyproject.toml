[tool.poetry]
name = "app_config"
version = "1.0.2"
description = ""
license = "MIT"
authors = []
packages = [{include = "app_config"}]

[tool.poetry.dependencies]
python = ">=3.11,<4"
dotenv = ">=0.9.9,<0.10.0"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
